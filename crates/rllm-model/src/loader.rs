use anyhow::Result;
use std::path::Path;

/// Load model weights from a local directory or Hugging Face model ID.
/// Placeholder — actual SafeTensors loading will be implemented in Phase 4.
pub fn load_weights(_model_path: &Path) -> Result<()> {
    Ok(())
}
